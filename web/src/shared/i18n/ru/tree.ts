// Раздел каталога, который грузится вместе со своим экраном (см. index.ts).
// Вид «Дерево» доски (этап 32.6).
import { plural } from '../../lib/plural.ts'

export const tree = {
    label: 'Дерево работы доски',
    intro:
      'Эпики, фичи и задачи доски — иерархией, вместе с их частями на других досках. Отбор здесь не действует: дерево без половины веток не отвечает, далеко ли до эпика.',
    empty: 'На доске нет ни одной карточки с частями — дерева пока нет.',
    alone: (n: number) => `Без родителя и без частей: ${n} ${plural(n, 'карточка', 'карточки', 'карточек')}`,
    toggle: (title: string, open: boolean) => `${open ? 'Свернуть' : 'Раскрыть'} «${title}»`,
    blocked: 'заблокирована',
    done: 'готово',
}
