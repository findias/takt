// Package docsrc — исходники документации, вшитые в бинарник.
//
// Из них сервер собирает справку внутри приложения (ROADMAP 30.3).
// Вшиваются исходники, а не собранный docs/html: справка обязана быть
// той же версии, что приложение, а собранные страницы подписаны версией
// того, кто последним запускал `make docs`. Собранная из вшитых
// исходников справка — тот же артефакт, что код, и из того же коммита.
//
// Пакет лежит здесь, а не в internal: `go:embed` видит только файлы
// ниже своего каталога.
package docsrc

import "embed"

//go:embed *.md ru/*.md screenshots/*.png ru/screenshots/*.png
var FS embed.FS
